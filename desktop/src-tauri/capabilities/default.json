{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Custom invoke commands (list_requests, read_request, run_collection) are allowed by default in Tauri v2.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
