/target
**/*.rs.bk
Cargo.lock.orig

# Tauri / node (desktop)
desktop/node_modules
desktop/dist
desktop/src-tauri/target
desktop/src-tauri/gen

# secrets must never land in git
*.secret
.env

# local editor/agent settings
.claude/settings.local.json

protoglot-spec.md

# internal planning docs (not published)
DEFERRED.md
